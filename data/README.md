# Local sample data: prerecorded traffic clips, test images, fixtures.
# Media blobs here are git-ignored (see root .gitignore) — keep large files
# out of the repo. Committed: this README and small fixtures only.
